name: Build Firmware(s)

on:
  push:
  schedule:
    - cron: "30 13 * * *"
  workflow_dispatch:

jobs:
  build:
    name: Build ESPHome binary for ${{ matrix.file }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        file:
          - the_brewery--air-quality-main.yaml
          - the_brewery--bt-proxy-bedrooms-poe.yaml
          - the_brewery--bt-proxy-office-poe.yaml
          - the_brewery--cat-laser.yaml
          - the_brewery--cat-wheel-trainer.yaml
          - the_brewery--cat-litter-box.yaml
          - the_brewery--co2-office.yaml
          - the_coastal_brewhouse--air-quality-main.yaml
          - the_coastal_brewhouse--airconditioner.yaml
          - the_coastal_brewhouse--bt-proxy.yaml
          - the_coastal_brewhouse--rf-proxy.yaml
          - the_coastal_brewhouse--washerdryer.yaml
          - the_coastal_brewhouse--waterheater.yaml
          - the_brewery--fireplace.yaml
          - the_brewery--garage.yaml
          - the_brewery--outdoor-patio-outlets.yaml
          - the_brewery--outdoor-patio-shades.yaml
          - the_ranch--bt-proxy.yaml
          - the_brewery--rf-proxy.yaml
    outputs:
      esphome-version: ${{ steps.esphome-build.outputs.esphome-version }}
    steps:
      - uses: actions/checkout@v4
      - name: Decrypt Secrets
        env:
          SECRET_YAML: ${{ secrets.SECRET_YAML }}
        run: |
          echo "$SECRET_YAML" > secrets.yaml
      - uses: esphome/build-action@v6
        id: esphome-build
        with:
          yaml-file: ${{ matrix.file }}
          version: latest
      - run: |
          mkdir output
          mv "${{ steps.esphome-build.outputs.name }}" output/
      - uses: actions/upload-artifact@v4
        with:
          name: esphome-${{ steps.esphome-build.outputs.name }}
          path: output
